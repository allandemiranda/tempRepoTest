package org.example.warming;

import java.awt.Point;

/**
 * 5. RandomWalk
 */
public class RandomWalk {
  /**
   To encode a random walk on a plane, we adopt the following convention:
   L: take a step left
   R: take a step right
   U: take a step up
   D: take a step down

   Let the point of origin (x = 0, y = 0)
   Let a trajectory : T0 = [L, L, U, R, D ..... R]
   1/ implement an algorithm to apply this random walk
   and gives the destination coordinates (x, y)

   2/ implement an algorithm to calculate maximum of trajectory: find the highest point

   3/ implement an algorithm to calculate minimum of trajectory: find the lowest point
   */

  public Point apply(Point startPoint, char[] trajectory) {
    //TODO
    return null;
  }

  public int max(Point startPoint, char[] trajectory) {
    //TODO
    return 0;
  }


  public int min(Point startPoint, char[] trajectory) {
    //TODO
    return 0;
  }
}
